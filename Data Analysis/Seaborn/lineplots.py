import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# first, read the data
# data_name = pd.read_csv('filename.csv')
# to set a specific index column, pass in the argument index_col = 'columnName'
# if you want dates to be automatically converted from string to date object, use argument parse_dates = True

# plot line chart using sns.lineplot(data = data_name)
# the method is sns.lineplot()
# data = 'whatever variable name the data is stored in'

# to plot only one column, use sns.lineplot(data = data_name['columnName'], label = 'columnName')
# can do this multiple times to plot select columns

# to change width and height of the figure, use plt.figure(figsize = (width, height))

# to add title, use plt.title('title')
# to add x axis label, use plt.xlabel('axis label')
# to add y axis label, use plt.ylabel('axis label')


# finally, to show plot, use plt.show()


# BASICALLY, ALL MATPLOTLIB COMMANDS WORK
